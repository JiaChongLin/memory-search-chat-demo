# Docs

`docs/` 目录记录项目设计与阶段性说明。当前文档已经同步到“阶段 2：两层权限规则接入聊天上下文解析”。

## 文档索引

- `api.md`
  说明当前 API，包括聊天接口、项目接口、会话管理接口，以及新的聊天上下文调试字段。
- `architecture.md`
  说明当前分层、数据模型关系，以及权限规则在 `MemoryService` 中的落点。
- `project-plan.md`
  记录阶段划分与当前进度，明确阶段 1 和阶段 2 已完成、allowlist 仍未实现。
- `dev-notes.md`
  记录当前权限判定约定、archived 的处理方式和后续扩展建议。

## 当前状态

当前仓库已经完成：

- 会话级聊天与摘要记忆
- 搜索触发与搜索结果注入
- 项目层 `Project` 数据模型
- 会话层状态管理与基础管理 API
- 聊天时的项目层默认边界 + 会话层收紧规则

当前仓库尚未完成：

- allowlist
- 向量库检索
- 更复杂的跨会话召回排序

如果你要继续做下一阶段，建议先看 `project-plan.md` 和 `dev-notes.md`。
